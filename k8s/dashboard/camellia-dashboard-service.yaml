apiVersion: v1
kind: Service
metadata:
  name: camellia-dashboard
spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30881
  selector:
    app: camellia-dashboard
